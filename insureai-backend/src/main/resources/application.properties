spring.application.name=insureai-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/insurancedb
spring.datasource.username=root
spring.datasource.password=mrudula1110

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration (with default fallback for local development)
jwt.secret=${JWT_SECRET:mySecretKeyForInsureAIBackendApp1234567890}
jwt.expiration=86400000

# Server Configuration
server.port=8081